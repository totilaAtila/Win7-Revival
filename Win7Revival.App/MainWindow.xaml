<Window
    x:Class="Win7Revival.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win7Revival.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <StackPanel Margin="20">
            <TextBlock Text="Win7Revival - Setări" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>

            <TextBlock x:Name="AdminWarning"
                       Text="Aplicația NU rulează ca Administrator. Funcționalitatea poate fi limitată."
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="OrangeRed"
                       Visibility="Collapsed"
                       Margin="0,0,0,15"/>

            <ItemsControl x:Name="ModulesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,8">
                            <ToggleSwitch Header="{Binding Name}"
                                          IsOn="{Binding IsEnabled, Mode=OneWay}"
                                          Toggled="ToggleSwitch_Toggled"/>
                            <TextBlock Text="{Binding Description}"
                                       VerticalAlignment="Center"
                                       Margin="10,0,0,0"
                                       Opacity="0.6"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <TextBlock Text="Notă: Pentru funcționalitate completă, rulați ca Administrator."
                       Style="{StaticResource CaptionTextBlockStyle}"
                       Foreground="{ThemeResource SystemFillColorCautionBrush}"
                       Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>
