<Window
    x:Class="Win7Revival.App.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Win7Revival.App"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <StackPanel Margin="20">
            <TextBlock Text="Win7Revival - Setări" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,20"/>
            
            <!-- Module List will be populated here -->
            <ItemsControl x:Name="ModulesList">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Margin="0,5">
                            <ToggleSwitch Header="{Binding Name}" IsOn="{Binding IsEnabled, Mode=TwoWay}" Toggled="ToggleSwitch_Toggled"/>
                            <TextBlock Text="{Binding Description}" VerticalAlignment="Center" Margin="10,0,0,0" Opacity="0.6"/>
                        </StackPanel>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
            
            <TextBlock Text="Notă: Aplicația trebuie rulată ca Administrator pentru a modifica Taskbar-ul." 
                       Style="{StaticResource CaptionTextBlockStyle}" 
                       Foreground="Red" 
                       Margin="0,20,0,0"/>
        </StackPanel>
    </Grid>
</Window>
